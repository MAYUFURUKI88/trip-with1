<%# 企画の概要 %>
<div class="item-show">
  <div class="item-box">
    <h2 class="name">
      <%= @plan.title %>
    </h2>
    <div class='item-img-content'>
      <%= image_tag @plan.image ,class:"item-box-img" %>
      
      <%
=begin%>
 <% if @pran.user_item.present? %>
      <div class='sold-out'>
        <span>Sold Out!!</span>
      </div>
      <% end %> 
<%
=end%>
<%
=begin%>
 受付終了の表示 
<%
=end%>
      
    </div>
    <div class="item-price-box">
      
      <span class="item-price">
        <%= @plan.first_day %>
      </span>
〜
      <span class="item-price">
        <%= @plan.final_day %>
      </span>
    </div>

    
    <%
=begin%>
 <% if user_signed_in? && current_user.id == @item.user_id && @item.user_item == nil %>
        <%= link_to '企画の編集', edit_item_path , method: :get, class: "item-red-btn" %>
        <p class='or-text'>or</p>
        <%= link_to '削除', item_path(@item.id), method: :delete, class:'item-destroy' %> 
<%
=end%>
   
    
 <%
=begin%>
 <%elsif @item.user_item == nil%> 
<%
=end%>
      <%= link_to '旅行に参加!',root_path, class:"item-red-btn"%>
       <%
=begin%>
 <%end%>  
<%
=end%>

     
     

    <div class="item-explain-box">
      <span><%= @plan.message %></span>
    </div>
    <table class="detail-table">
      <tbody>
        <tr>
          <th class="detail-item">企画者</th>
          <td class="detail-value"><%= @plan.user.nickname %></td>
        </tr>
        <tr>
          <th class="detail-item">旅行先</th>
          <td class="detail-value"><%= @plan.trip.name %></td>
        </tr>
        <tr>
          <th class="detail-item">定員</th>
          <td class="detail-value"><%= @plan.capacity.name %></td>
        </tr>
        <tr>
          <th class="detail-item">締切日</th>
          <td class="detail-value"><%= @plan.deadline %></td>
        </tr>
        <tr>
          <th class="detail-item">金額</th>
          <td class="detail-value"><%= @plan.price %></td>
        </tr>
      </tbody>
    </table>
